package com.quew8.geng3d.rendering;

/**
 *
 * @author Quew8
 */
/*public class SpriteBatcher3D extends SpriteBatcher<SpriteDataFactory3D> {

    public SpriteBatcher3D(Texture tex, StaticRenderMode renderMode, SpriteBatcherData<SpriteDataFactory3D> data, SpriteIndexDataFactory indexFactory) {
        super(tex, renderMode, data, indexFactory);
    }
    
    public void draw(Image img, float x, float y, float z, float width, float height, float depth, Plane plane) {
        predraw();
        for(int i = 0; i < getAllAttribs().length; i++) {
            getAllAttribs()[i].getFactory().addData(getAllAttribs()[i].getBuffer(), img, x, y, z, width, height, depth, plane);
        }
    }
    
    public void draw(Colour colour, float x, float y, float z, float width, float height, float depth, Plane plane) {
        predraw();
        for(int i = 0; i < getAllAttribs().length; i++) {
            getAllAttribs()[i].getFactory().addData(getAllAttribs()[i].getBuffer(), colour, x, y, z, width, height, depth, plane);
        }
    }
    
    public void draw(float x, float y, float z, float width, float height, float depth, Plane plane) {
        predraw();
        for(int i = 0; i < getAllAttribs().length; i++) {
            getAllAttribs()[i].getFactory().addData(getAllAttribs()[i].getBuffer(), x, y, z, width, height, depth, plane);
        }
    }
}*/
